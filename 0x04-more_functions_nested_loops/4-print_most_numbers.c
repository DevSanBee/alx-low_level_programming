#include "main.h"
/**
 * print_most_numbers = Entry point
 * Return: nothing
 */
void print_most_numbers(void)
{
	char d;

	for (d = 48; d < 58; d++)
	{
		if (d != 50 && d != 52)
			_putchar(c);
	}
	_putchar('\n');
}
